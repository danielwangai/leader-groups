<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %>
    <%= f.text_field :username %>
  </div>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :role_name %>
    <%= collection_select :user, :role_id, Role.all, :id, :role_name, {prompt: true} %>
  </div>

  <div class="field">
    <%= f.label :"Group name" %>
    <%= collection_select :user, :group_id, Group.all, :id, :name, {prompt: true} %>
  </div>

  <div class="field">
    <%= f.hidden_field :password, value: 12345678 %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
